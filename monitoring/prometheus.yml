global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: "layra-backend"
    metrics_path: /api/v1/health/metrics
    static_configs:
      - targets: ["backend:8000"]
  # Qdrant removed (2026-02-02) - Milvus is now sole vector DB
